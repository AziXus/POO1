package engine.pieces;

import chess.PlayerColor;
import engine.Square;

public class Knight extends Piece {
    public Knight(PlayerColor playerColor, Square position) {
        super(playerColor);
    }
}
