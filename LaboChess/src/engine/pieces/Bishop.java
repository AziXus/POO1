package engine.pieces;

import chess.PlayerColor;
import engine.Square;

public class Bishop extends Piece {
    public Bishop(PlayerColor playerColor, int X, int Y) {
        super(playerColor, X, Y);
    }
}
